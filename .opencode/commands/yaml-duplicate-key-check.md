---
description: Detect duplicate YAML keys for one or more files; exits non-zero on violations.
---

Run the canonical YAML duplicate-key checker. It is read-only and fails when any duplicate key is found.

```bash
bash scripts/yaml-duplicate-key-check.sh $ARGUMENTS
```
