# CRD and Helm Sync

## When to Run
- Changes under `repo/longhorn-manager/pkg/apis/...` or CRD definitions.

## Commands (reference)
```sh

If using the skill `sync-crd-helm`, follow its defensive flags (`--execute/--force`, dry-run default).
